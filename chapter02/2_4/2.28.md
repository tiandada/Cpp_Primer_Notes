```
(a) int i, *const cp;
//定义了一个int型的变量i，和一个常量指针cp，cp指向一个int型的变量
(b) int *p1, *const p2;
//定义了一个指针p1，和一个常量指针p2, p2指向一个int型的变量
(c) const int ic, &r = ic;
//定义了一个常量ic，和一个
(d) const int *const p3;
(e) const int *p;
```